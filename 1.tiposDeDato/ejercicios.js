//averiguar metedo de javascript
//que me permite saber que tipo de dato 
//tengo

/*typeof 
    typeof El operador typeof devuelve una cadena que indica el tipo del operando sin evaluarlo. 
    operando es la cadena, variable, palabra clave u objeto para el que se devolverá su tipo. Los paréntesis son opcionales.
    Para las palabras clave true y null, el operador typeof devuelve los siguientes resultados:
    que me permite saber que tipo de dato tengo*/

    var tipo=typeof("hola")
    console.log(tipo)

    var tipo=typeof(9)
    console.log(tipo)
    

    //typeof true === 'boolean'
    //typeof null === 'object'
    //typeof 62 === 'number'
    //typeof 'Hola mundo' === 'string'
